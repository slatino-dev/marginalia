# BUILD PLAN: Marginalia

> Model routing (updated 2026-07-08 per Sam): Fable 5 xhigh does planning, idea generation, and adversarial review ONLY (never security content). Opus 4.8 high does ALL implementation, drafting, and fixes. ALL security work (design, planning, implementation, review) is Opus-owned. No agent fan-outs without Sam's approval + expected agent count.

## Phase 0 — Foundation (gate: CI green on hello-world Worker + SPA)
Repo `slatino-dev/marginalia` public from day one · wrangler + vitest-pool-workers scaffold · drizzle + D1 migrations pipeline · CI: types/lint/unit on push, wrangler deploy on main · typed `analytics/events.ts` with the `citation_audit_opened` activation event · DESIGN.md + theme.css inheriting the shared Signal Path token base built by Conduit (which builds before Marginalia and creates it de facto — Marginalia does NOT choose a second type pairing; as the flagship design piece it may instead ratify/harden that base), then layering only the Rubric vermilion + annotated-page overrides (Design Loop steps 2 to 3) · **re-verify every Cloudflare limit cited in ARCHITECTURE.md and record number + read date** (D1, KV, Workers requests, DO, R2, CPU-ms not yet verified this session). **Evidence**: green CI run URL, deployed hello-world Worker responding, migration applied against miniflare D1 in a test.

## Phase 1 — Corpus + ingest (gate: corpus queryable in D1/Vectorize/R2 with measured counts)
Offline ingest pipeline (fetch pinned PDFs -> sha256 verify -> per-page extract -> structure-aware ~400-tok chunking with section paths + page anchors + structural headers -> Workers AI REST embed -> batch upsert) · FTS5 external-content table + BM25 queries tested in vitest-pool-workers · Vectorize index + batch upserts · embedding-model decision made by the ARCHITECTURE.md sizing rule from the MEASURED chunk count · docs/corpus-dossier.md completed with sources, sha256s, actual page counts, and the 17 U.S.C. 105 public-domain basis. · **begin DRAFTING the >=60-question golden set now** (question text + `expected_answer_notes` only; `expected_chunk_ids` labeling is deferred to the Phase 3 ingest-gated step per docs/eval-spec.md) so the schedule's long pole starts before ingest lands · [SECURITY/Opus] admin/ingest authentication + isolation from public routes · [SECURITY/Opus] pinned source-URL allow-list + checksum-abort on mismatch · [SECURITY/Opus] append-only `admin_audit` trail (actor/action/target/ts/reason) on ingest runs, extended to trace-redaction + feedback-moderation + eval-config actions as those surfaces land in Phase 3. **Evidence**: measured chunk + vector counts from queried D1/Vectorize, a BM25 and a dense query each returning expected passages in a test transcript, dossier committed with real sha256s.

## Phase 2 — v0.5 retrieval core (gate: Sam asks a novel question and audits a citation to the exact source passage)
Hybrid search endpoint via the Backend Loop per endpoint (SPEC -> SCHEMA -> TEST -> BUILD -> VERIFY): FTS5 BM25 lane + Vectorize dense lane -> reciprocal-rank fusion -> bge-reranker-base -> top-8 · single-pass llama-3.3-70b synthesis with inline citation binding + resolution validation (unresolved markers stripped and counted) · full trace persistence (retrieval_steps, rerank_results, answers, queries written atomically) · answer view with margin notes + audit panel + a STATIC trace rendering (the self-drawing trace animation is v1 polish, deferred to Phase 3; Design Loop steps 4 to 8, screenshots at 390/768/1440 from this session) · KV preset full-trace cache serving <1s TTFT · neuron-budget DO with a **hard combined+per-visitor cap that 429s at the ceiling** (the full four-rung degrade LADDER is v1 polish, deferred to Phase 3 — the deploy-on-main URL is public, so the hard cap is the justified minimum here) · **CPU-ms gate: measure actual `cpuTime` on a real agentic-shaped `/ask` via Workers Logs and confirm it is under the 10ms budget** (if not, move the agent loop into a DO per the ARCHITECTURE fallback before proceeding) · [SECURITY/Opus] per-visitor + global (4,000/day combined ceiling from the portfolio allocation) query budgets enforced in the DO before model calls · [SECURITY/Opus] rate limits with `RateLimit-*` headers on `/ask` and all public routes · [SECURITY/Opus] per-call `max_tokens` caps on every model call · [SECURITY/Opus] CSP + security headers + request body-size limit + same-origin CORS on SPA/API responses · [SECURITY/Opus] prompt-injection posture review (untrusted question + corpus wrapping); all model/corpus text rendered as sanitized markdown (Streamdown), no raw HTML. **Evidence**: screenshots of a novel question answered and a citation audited to the exact Rogers Commission passage, queried D1 trace rows, a preset-cache <1s TTFT transcript, a measured `cpuTime` figure from Workers Logs, a 429 transcript at the cap, a passing failing-then-green test for citation resolution.

## Phase 3 — v1 agentic + eval tab (gate: quality gates + Opus security review pass + eval tab showing >=7 consecutive nightly runs)
Agent loop (8b planner -> per-sub-query retrieval -> sufficiency under a 4-step budget -> 70b synthesis) streamed as the typed SSE event protocol, rendered per the agentic-UI system · promptfoo harness + the 60-question golden set (docs/eval-spec.md: question authoring rules, expected-chunk labeling, versioned judge prompts). **This is the schedule's critical-path long pole**: the primary metric needs >=7 consecutive nightly `eval_run` rows by 09-08, so the cron+harness must be stable by ~09-01, which needs the golden set's `expected_chunk_ids` labeled by READING ~1,000+ pages of dense source — and that labeling is gated behind the Phase 1 ingest fixing the chunk boundaries. Questions are DRAFTED in Phase 1 (above) so only the label-binding waits on the ingest gate; if 62 days runs tight the v0.5 date (08-18) is the earlier canary. Any re-chunking event forces relabeling of the affected questions (a labeled `config_hash` discontinuity, never a silent series break) · ONE nightly eval cron dispatching internally to retrieval (nightly) + faithfulness (5 questions/night on 4 assigned nights), with a "didn't run" alert (budget reset moved to a DO alarm, not a cron) · ablation runs (hybrid vs dense vs BM25, and the flagship structural-vs-LLM-contextual-headers experiment) · eval tab UI (TanStack Table + dataviz-compliant charts, reading only committed eval_runs) · the full four-rung degrade ladder as designed states (multi-mode + honest banner copy, deferred from Phase 2) · the self-drawing trace-schematic animation (the hero motion moment, transform/opacity only, reduced-motion static fallback; deferred from Phase 2) · share permalinks replaying stored traces · citation feedback · [SECURITY/Opus] feedback-endpoint abuse/spam protection, no PII · [SECURITY/Opus] full `security-review` skill pass over ask path, admin surface, feedback, and eval cron. **Evidence**: the live eval tab with >=7 real nightly eval_run rows, axe scan clean on changed views, Lighthouse budget pass, a security-review report from this session.

## Phase 4 — Launch (gate: launch checklist complete; success-metric instrumentation live)
Docs + changelog pages · robots.txt AI-crawler allows (verify Cloudflare's default AI-crawler block is OFF for the zone) · OG cards · PostHog dashboards: activation funnel (`citation_audit_opened`), TTV, D7, degrade-mode frequency · corpus dossier published as a docs page (the Postmortem Library is itself linkable content) · Show HN: "RAG that grades itself in public," the eval tab as the story · v1.x MCP surface planned for Patchbay tenancy · [SECURITY/Opus] MCP endpoint auth designed and implemented when that surface ships. **Evidence**: launch checklist run, tracking events firing in PostHog captured this session, the deployed marginalia.samlatino.dev responding.

## Verification gates (every phase)
Evidence from the session or it did not happen: failing-then-passing vitest-pool-workers runs, real curl/SSE transcripts against the deployed Worker, D1 rows queried, screenshots from this session, eval_run rows on the live tab. Two failed corrections on one problem stops the loop for a rethink, never a third blind attempt. Once launched, the weekly agentic growth review (headless, Opus, budget-capped, JSON output, locked, "didn't run" alert) proposes and Sam disposes.
